import 'package:flutter/material.dart';

class usuarios_page extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Column(
          children: [
            Text("usuarios_page")
          ],
        ),
      ),
    );
  }
}
